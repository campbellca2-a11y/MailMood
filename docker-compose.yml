services:
  mailmood-api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: mailmood-api
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
    restart: unless-stopped
